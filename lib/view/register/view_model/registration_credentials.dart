class RegistrationCredentials {
  String email, username, password, validationPass;
  RegistrationCredentials(
      {required this.email,
      required this.password,
      required this.username,
      required this.validationPass});
}
